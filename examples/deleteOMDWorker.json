{
  "name": "Delete Worker",
  "description": "Deletes specific worker from omd-dev",
  "command": "kubectl get pods -n=omd-worker | awk '{print $1}' | grep name | xargs kubectl delete pod -n=omd-worker",
  "script": false,
  "path": "/bin/bash",
  "interactive": false,
  "params": {
    "name": "omd-dev-db"
  }
}